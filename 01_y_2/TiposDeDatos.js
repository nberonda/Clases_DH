// Dato string (entre comillas simples o dobles)

let nombre = 'Nicolás'
console.log (nombre)
let apellido = "Beronda"
console.log (apellido)

// Datos booleanos:

let verdadero = true;
console.log (verdadero)
let falso = false;
console.log (falso)

// Dato NaN (Not a Number)

let notANumber = NaN;
console.log (notANumber)

// Dato null

let nulo = null;
console.log (nulo)

// Dato undefined

let sinValor;
console.log (sinValor)

// Dato objeto literal

let persona = {
    nombre: 'Nicolás',
    edad: 20,
    altura: 1.73}
    console.log (persona);

//dato array

let paises = [ 'Argentina', 'Venezuela', 'Colombia'];
console.log (paises)